# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=grammar_agent
DB_USERNAME=grammar_agent
DB_PASSWORD=replace_with_a_strong_local_password

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=replace_with_a_strong_local_password

# JWT (reserved for a later authentication phase)
JWT_SECRET=replace_with_a_random_secret_of_at_least_32_bytes
JWT_ACCESS_EXPIRE=900
JWT_REFRESH_EXPIRE=2592000

# Application
SERVER_PORT=8080
